<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    {# %'s tell compiler to pull a file, not front matter #}
    {% include 'header.njk' %}

    {# content is a special case, will pull in everything after the front matter #}
    <main>

    {{ content | safe }} 


    </main>

    {% include 'footer.njk' %}

</body>
</html>